# Skills

Each skill folder documents a single, simple workflow.

- `run-local/skill.md`: run backend + build/serve SPA
- `rekey/skill.md`: rotate master password (re-encrypt all entries)
- `frontend-build/skill.md`: build SPA into `vellichor/static/app/`
- `project-goal/skill.md`: project goal + key modules overview

